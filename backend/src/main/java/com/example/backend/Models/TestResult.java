package com.example.backend.Models;

public class TestResult {
    // To be used to keep track of results received from client.  
    // This will contain an array of TestWords  

    TestWord[] fullTest;
    Integer wordsPerMinute;

    // Consider: is it worth it to allow the user to store the full test of words that they typed? How can it be stored? (prefer this)
    // Or should we just store the stats?

}
